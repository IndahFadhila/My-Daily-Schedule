{
  "name": "jadwal-push",
  "private": true,
  "type": "module",
  "scripts": {
    "gen-vapid": "node scripts/generate-vapid.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  }
}
